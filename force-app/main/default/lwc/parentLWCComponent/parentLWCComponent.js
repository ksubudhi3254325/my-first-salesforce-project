import { LightningElement } from 'lwc';

export default class ParentComponent extends LightningElement {
    // This value is passed dynamically into the second child component
    parentDashboardTitle = 'Marketing Team Tracker';
}